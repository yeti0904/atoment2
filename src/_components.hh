#pragma once
#include <cstdint>
#include <string>
#include <vector>
#include <fstream>
#include <variant>
#include <unordered_map>

using std::get;
using std::string;
using std::vector;
using std::variant;
using std::ifstream;
using std::ofstream;
using std::to_string;
using std::unordered_map;

typedef uint32_t ATM_Integer;